// Copy this file to secrets.h and fill in your values.
// secrets.h is gitignored and will not be committed.
//
// WiFi credentials live in .env (WIFI_SSID / WIFI_PASS), not here.

#ifndef SECRETS_H
#define SECRETS_H

// BLE sessions do not bond. Uncomment to require passkey entry on every new
// connection in addition to BLE_AUTH_TOKEN (usually unnecessary on macOS).
// #define BLE_USE_PASSKEY 1
// WARNING: Change the default passkey for security
// #define BLE_PASSKEY 123456  // 6-digit (000000–999999); used when BLE_USE_PASSKEY is enabled

#endif
